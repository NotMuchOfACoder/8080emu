lea    a1, #$1000        ; the address of variable a    
   lea    a2, #$1008        ; the address of variable b    
   move.l d0,(a2)    variable b    
   move.l d0,(a2)    
   add.l  d0, #$64    

   add.l  d0, #$64    
